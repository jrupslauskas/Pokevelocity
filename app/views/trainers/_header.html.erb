<nav class="bg-red-600 shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center space-x-8">
        <%= link_to dashboard_path, class: "text-2xl font-bold text-white hover:text-yellow-300 transition-colors" do %>
          Pokévelocity
        <% end %>

        <!-- Navigation Links -->
        <div class="hidden md:flex space-x-4">
          <%= link_to pokedex_path, class: "text-white hover:text-yellow-300 font-semibold transition-colors px-3 py-2 rounded-lg hover:bg-red-700" do %>
            📖 Pokédex
          <% end %>
          <%= link_to catch_pokemon_path, class: "text-white hover:text-yellow-300 font-semibold transition-colors px-3 py-2 rounded-lg hover:bg-red-700" do %>
            🎣 Catch
          <% end %>
          <%= link_to bag_path, class: "text-white hover:text-yellow-300 font-semibold transition-colors px-3 py-2 rounded-lg hover:bg-red-700" do %>
            🎒 Bag
          <% end %>
          <%= link_to rewards_path, class: "text-white hover:text-yellow-300 font-semibold transition-colors px-3 py-2 rounded-lg hover:bg-red-700" do %>
            🎁 Redeem
          <% end %>
        </div>
      </div>

      <div class="flex items-center space-x-4">
        <span class="text-white font-semibold"><%= current_trainer.username %></span>
        <%= button_to "Logout", logout_path, method: :delete,
            class: "bg-white text-red-600 px-4 py-2 rounded-lg font-semibold hover:bg-gray-100 transition-colors" %>
      </div>
    </div>

    <!-- Mobile Navigation -->
    <div class="md:hidden pb-4 grid grid-cols-4 gap-2">
      <%= link_to pokedex_path, class: "text-center text-white hover:text-yellow-300 font-semibold transition-colors px-2 py-2 rounded-lg hover:bg-red-700 text-sm" do %>
        📖 Pokédex
      <% end %>
      <%= link_to catch_pokemon_path, class: "text-center text-white hover:text-yellow-300 font-semibold transition-colors px-2 py-2 rounded-lg hover:bg-red-700 text-sm" do %>
        🎣 Catch
      <% end %>
      <%= link_to bag_path, class: "text-center text-white hover:text-yellow-300 font-semibold transition-colors px-2 py-2 rounded-lg hover:bg-red-700 text-sm" do %>
        🎒 Bag
      <% end %>
      <%= link_to rewards_path, class: "text-center text-white hover:text-yellow-300 font-semibold transition-colors px-2 py-2 rounded-lg hover:bg-red-700 text-sm" do %>
        🎁 Redeem
      <% end %>
    </div>
  </div>
</nav>
